# webpage_Eleven_AIMES 감사 보고서 (2026-05-31)

## 개요

본 프로젝트는 AIMES(11개 산업 제조실행시스템) 소개용 정적 웹사이트이다. 빌드 도구나 패키지 매니저 없이 순수 HTML/CSS/JS로 구성된다.

- 구성 파일(확인): `index.html`(1,855행), `style.css`(1,397행), `script.js`(794행), `README.md`(304행). 총 4개 파일.
- `package.json` 부재(확인) → 빌드/tsc/lint 대상 없음. README의 실행 방법은 `python3 -m http.server` 또는 `npx serve` 기반의 정적 서빙이다(확인).
- 외부 의존성(확인): CDN으로 Bootstrap 5.3.2, Bootstrap Icons 1.11.1, Google Fonts(Outfit/Inter), Three.js 0.160.0, GSAP 3.12.5 + ScrollTrigger를 로드한다.
- 로컬 자산 참조(확인): `style.css`, `script.js` 2건뿐이며 모두 실제 존재한다.
- 기능(확인, 정적 코드 판독 기준): Three.js 파티클 배경, GSAP 스크롤 애니메이션, 카운터, 스무스 스크롤, 도메인 카드 모달, KR/EN i18n 전환.

## 실행·테스트 결과

서버 미기동·읽기 전용 원칙을 준수하였다. 정적 프로젝트이므로 브라우저 런타임 실행은 수행하지 않았고, 정적 검증만 실시하였다.

- `node --check script.js`: 통과(확인). 문법 오류 없음.
- 로컬 자산 참조 무결성: `href="style.css"`, `src="script.js"` 2건 모두 파일 존재 확인(확인).
- 내부 앵커 무결성: 네비게이션·푸터의 `href="#..."` 대상 9개(ai-pipeline, architecture, comparison, domains, project-status, roi, security, techstack, workflow)가 모두 대응하는 `id`로 존재(확인).
- HTML 태그 균형(개략): `<div>` 393 / `</div>` 393, `<section>` 13 / `</section>` 13으로 균형(확인). 단, grep 기반 개략 집계이므로 완전한 파서 검증은 아님(추정).
- i18n 키 정합성: `data-i18n` 고유 키 194개 전부에 대해 `script.js`의 EN 번역 항목이 존재(확인). 누락 키 없음.
- 브랜드 스크럽 검사(case-insensitive: `wdlab`/`wdlab`/`WDLAB@2023-2026`/`wdlab`/`wdlab`/`wdlab`/`a3-ai`/`wdlab`): 조치 후 잔존 0건(확인). `A3DE`/`A3-ADE`는 애초에 미사용(확인).

## 발견된 문제점 (확인 vs 추정, 심각도)

1. (확인 / 심각도: 중) 브랜드 스크럽 누락 잔존물. `README.md` 5행에 `WDLAB@2023-2026 Working Group`이 남아 있었다. 스크럽 유지 대상(`A3DE`/`A3-ADE`)에 해당하지 않으며, HTML/JS 푸터는 동일 조직을 `WDLAB@2023-2026 Working Group`으로 표기하므로 명백한 미치환 잔존물로 판단된다.
2. (확인 / 심각도: 하) 브랜드 스크럽 중복 치환 아티팩트. `index.html` 1803행과 `script.js` 503행의 푸터 "설계" 문구에서 토큰이 `WDLAB@2023-2026 WDLAB@2023-2026`로 이중 출력되었다. 서로 다른 두 원본 브랜드('WDLAB@2023-2026'와 'WDLAB@2023-2026')가 동일 문자열로 치환되며 중복이 발생한 것으로 판단된다.
3. (확인 / 심각도: 하) 저작권 연도 불일치. `README.md` 304행은 `© 2025`, `index.html` 1833행 푸터는 `© 2026`이다. 브랜드 문제 아님, 콘텐츠 정합성 이슈.
4. (추정 / 심각도: 하) `README.md` 21~23행의 줄 수 기재(index 1,647 / style 1,338 / script 531행)가 실제 파일(1,855 / 1,397 / 794행)과 불일치한다. 문서 노후로 추정되며 기능 영향 없음.
5. (추정 / 심각도: 하/정보) 정량 수치(소스 2,394개, 테스트 1,313건 100% PASS, 마이크로서비스 96개 등)는 본 웹사이트가 소개하는 외부 AIMES 프로젝트군에 대한 주장으로, 본 저장소 내에서 검증 가능한 근거는 없다(검증 범위 밖).
6. (추정 / 심각도: 하) 헤로 섹션(`index.html` 65~66행)에 `WDLAB@2023-2026 Co., Ltd.`와 `WDLAB@2023-2026 Working Group` 두 태그가 인접 배치된다. 원본이 서로 다른 두 브랜드였던 것으로 보여 의도된 별개 항목으로 판단하였고, 중복 아티팩트로 보지 않아 보존하였다.

## 조치한 내용

저위험 항목에 한해 수정하고 재검증하였다.

1. `README.md` 5행: `WDLAB@2023-2026 Working Group` → `WDLAB@2023-2026 Working Group` (확인, 재검증 시 잔존 0건).
2. `index.html` 1803행: `WDLAB@2023-2026 WDLAB@2023-2026 워킹그룹` → `WDLAB@2023-2026 워킹그룹` (확인).
3. `script.js` 503행(`footer_by` EN): `WDLAB@2023-2026 WDLAB@2023-2026 Working Group` → `WDLAB@2023-2026 Working Group` (확인).

재검증 결과: 브랜드 잔존·이중 토큰 모두 0건, `node --check script.js` 통과, i18n 키 정합 유지(확인).

## 미해결·위험 항목

- (미조치, 권고) 저작권 연도 불일치(README © 2025 vs HTML © 2026): 정책상 어느 쪽이 정본인지 불명확하여 콘텐츠 변경은 보류하였다. 한쪽으로 통일 권고.
- (미조치, 권고) `README.md`의 파일 줄 수 기재값 갱신 권고. 기능 영향 없어 보류.
- (검증 범위 밖) 소개 대상 AIMES 프로젝트군의 정량 지표(테스트 100% PASS 등)는 본 저장소만으로 확인 불가. 외부 근거 확인 필요.
- (환경 한계) 브라우저 런타임 미실행으로 인해 CDN 의존 기능(Three.js/GSAP/Bootstrap)의 실제 동작 및 콘솔 오류 여부는 미관찰. 코드 자체에는 CDN 미로딩 대비 try/catch·typeof 가드가 존재(확인).

## 종합 판단

정적 사이트로서 구조는 건전하다. 로컬 자산·내부 앵커 무결성, JS 문법, i18n 키 정합성이 모두 확인되었다. 브랜드 스크럽 관련 실질 결함 2종(미치환 잔존 1건, 중복 토큰 2개소)을 발견하여 모두 수정·재검증하였고, 현재 브랜드 잔존물은 0건이다. 남은 항목은 저작권 연도·문서 줄 수 등 비기능적·콘텐츠성 불일치로 위험도는 낮다. 브라우저 실행 검증은 환경 제약으로 수행하지 않았다.
